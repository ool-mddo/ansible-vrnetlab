protocols {
    ospf {
        area 0.0.0.0 {
            interface eth2;
            interface eth3;
        }
        export ospf-redistribute;
    }
}
policy-options {
    policy-statement ospf-redistribute {
        from protocol [ direct static ];
        then accept;
    }
}
